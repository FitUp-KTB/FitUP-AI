version: "3.8"

services:
  fastapi-app:
    image: ${DOCKER_HUB_USERNAME}/fastapi-rag:latest
    container_name: fastapi-rag
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
